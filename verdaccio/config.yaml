storage: /verdaccio/storage

uplinks:
  npmjs:
    url: https://registry.npmjs.org/
    cache: true

packages:
  '@*/*':
    access: $all
    proxy: npmjs
  '**':
    access: $all
    proxy: npmjs

web:
  enable: true
  title: "Orochi NPM Cache"

listen: 0.0.0.0:4873

max_body_size: 100mb
